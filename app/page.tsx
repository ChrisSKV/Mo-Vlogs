import { Landing } from "@/components/landing";

/**
 * The control. Light ground, one dark panel for the prize.
 * The dark variant of the same page lives at /alt-1.
 */
export default function Page() {
  return <Landing />;
}
